// Database temporarily disabled - returning null to prevent build errors
// TODO: Replace with proper database solution for serverless environment

export function initDb() {
  // Database initialization disabled
  console.log('[v0] Database initialization skipped');
}

// Export null as db placeholder
const db = null;

export default db;
