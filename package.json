{
  "name": "xclusive-barber-booking-root",
  "private": true,
  "scripts": {
    "dev": "pnpm --prefix frontend dev",
    "dev:frontend": "pnpm --prefix frontend dev",
    "build": "pnpm --prefix frontend build",
    "build:frontend": "pnpm --prefix frontend build",
    "start": "pnpm --prefix frontend start",
    "lint": "pnpm --prefix frontend lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "lucide-react": "^0.577.0",
    "motion": "^12.36.0",
    "react": "^19.2.4",
    "react-day-picker": "^9.14.0",
    "sonner": "^2.0.7",
    "tw-animate-css": "^1.4.0"
  }
}
